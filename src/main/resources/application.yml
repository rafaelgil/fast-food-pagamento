server:
  port: 8090

spring:
  mongodb:
    embedded:
      version: 4.0.0
  main:
    allow-bean-definition-overriding: true


cloud:
  aws:
    stack:
      auto: false
    region:
      static: us-east-1
    credentials:
      access-key: localstack
      secret-key: localstack
    end-point:
      uri: https://sqs.us-east-1.amazonaws.com/372431383879

aws:
  queue:
    name: notificacao-pagamento-sync

mongodb:
  authentication-database: admin
  database: fast-food-pagamento
  url: ${MONGO_URL:mongodb://127.0.0.1:27017}
  username: ${MONGO_USER:root}
  password: ${MONGO_PASSWORD:rootpassword}